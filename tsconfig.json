/*
  Target Environment: The orchestration runtime

  Key Purposes:
  - Root file
  - Binds all sub-configs together via project references

  Target API or Libraries:
  - None directly
  - Points IDEs to other configs
*/
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
